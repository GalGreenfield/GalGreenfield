{
  "name": "sketchup-redesign-app",
  "version": "0.1.0",
  "private": true,
  "description": "Web app that accepts SketchUp models and returns a redesigned model based on a prompt.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "nanoid": "^5.0.7"
  }
}
